<resources>
    <!-- Should we listen for fingerprints when the screen is off?  Devices
         with a rear-mounted sensor want this, but certain devices have
         the sensor embedded in the power key and listening all the time
         causes a poor experience. -->
    <bool name="config_fingerprintWakeAndUnlock">true</bool>

    <!-- Max visible notification icons -->
    <integer name="config_maxVisibleNotificationIcons">4</integer>
    <integer name="config_maxVisibleNotificationIconsOnLock">5</integer>
    <integer name="config_maxVisibleNotificationIconsWhenDark">5</integer>

    <!-- Whether to show VoLTE icon in the status bar -->
    <bool name="config_display_volte">true</bool>

    <!-- Whether the binned brightness sensor reports a value of 0 on proximity
         near events -->
    <bool name="doze_brightness_sensor_reports_proximity" translatable="false">true</bool>

    <!-- Allow devices override audio panel location to the left side -->
    <bool name="config_audioPanelOnLeftSide">false</bool>

    <!-- Whether to use the mask for the quicksettings icons-->
    <bool name="config_useMaskForQs">true</bool>

    <!-- Whether the qs statusbar is hidden -->
    <bool name="qs_status_bar_hidden">false</bool>

    <!-- System icon area layout weight -->
    <item name="system_icon_area_weight" format="float" type="integer">1</item>

    <!-- CPUInfoService temperature sysfs path -->
    <string name="config_cpuinfoSysTemp" translatable="false"></string>
</resources>
